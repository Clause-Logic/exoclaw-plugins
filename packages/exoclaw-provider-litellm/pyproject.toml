[project]
name = "exoclaw-provider-litellm"
version = "0.3.0"
description = "LiteLLM provider for exoclaw"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "exoclaw>=0.8.0",
    "litellm>=1.0.0",
    "json-repair>=0.1.0",
    "structlog>=25.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

