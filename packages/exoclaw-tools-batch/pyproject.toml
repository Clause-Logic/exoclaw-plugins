[project]
name = "exoclaw-tools-batch"
version = "0.1.0"
description = "Batch/map tool for running tools against multiple inputs concurrently"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "exoclaw>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
