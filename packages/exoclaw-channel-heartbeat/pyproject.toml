[project]
name = "exoclaw-channel-heartbeat"
version = "0.2.1"
description = "Periodic heartbeat channel for exoclaw"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "exoclaw>=0.22.0",
    "structlog>=25.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
