[project]
name = "exoclaw-subagent"
version = "0.1.2"
description = "Subagent manager for exoclaw — nested agent execution via AgentLoop"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "exoclaw>=0.6.0",
    "structlog>=25.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
