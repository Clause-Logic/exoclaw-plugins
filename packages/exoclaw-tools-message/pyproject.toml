[project]
name = "exoclaw-tools-message"
version = "0.1.1"
description = "Message sending tool for exoclaw"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "exoclaw>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
